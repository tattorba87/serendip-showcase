# This repo holds documentation and media only — no build, no dependencies.
# Raw capture footage stays local; only the edited, reviewed exports are committed.
media/raw/
*.mov
.DS_Store

# Working plan for this repo — local only, not published.
PLAN.md
